# App + ML core
streamlit>=1.40
sentence-transformers>=2.2
transformers>=4.30,<5
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24,<2
tqdm>=4.64
requests>=2.31
pillow>=10.0
huggingface_hub>=0.21
pyarrow>=18

# FAISS (optional; remove if it fails to build)
faiss-cpu>=1.8

# Force CPU PyTorch (no CUDA wheels on Streamlit Cloud)
torch==2.2.2+cpu ; sys_platform != "darwin"
--extra-index-url https://download.pytorch.org/whl/cpu
